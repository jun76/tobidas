# 公開Web版のビルド時に、対象Origin用のWebMCP Origin Trialトークンを指定する。
# トークン自体は生成HTMLへ埋め込まれる公開値であり、作品データやAPIキーではない。
# サブドメイン対象とThird-party matchingはOFFにして発行する。
WEBMCP_ORIGIN_TRIAL_TOKEN=

# Edge側のOrigin Trialにも参加する場合だけ、同じOrigin向けのEdgeトークンを追加する。
WEBMCP_EDGE_ORIGIN_TRIAL_TOKEN=
