# AGENTS.md

このファイルは、tobidasを使って飛び出す絵本作品を作るエージェント向けの制作規約です。

tobidas本体のソース、保存形式、ビルド、デプロイを変更する場合は、作業前に `AGENTS_DEV.md` も読んでください。
作品データと素材だけを扱う場合は、このファイルだけで作業できます。

## tobidasでの作品制作

tobidasは、完全に開いた見開きを編集して「飛び出す絵本風」のWeb作品を作るローカルファーストのビルダーです。
開閉途中の姿勢はtobidasが導出するため、制作者は開いた状態の構図、部品、演出、音を決めます。

作品は次のフォルダ形式で保存します。

```text
my-book/
├─ project.json
└─ assets/
   ├─ background.webp
   ├─ character.webp
   └─ page-turn.wav
```

`project.json` と `assets/` を分離したフォルダが交換形式です。
作品固有のバージョンは持たず、保存形式の互換性はtobidas本体のリリースバージョンで管理します。

通常の作品制作にはブラウザのビルダーを使います。

```bash
npm install
npm run dev
```

`http://localhost:5174/` を開き、「新規作成」「開く」「保存」「エクスポート」を使います。
作品データと読み込んだ素材はブラウザ内で処理します。

## 構図と紙工作

制作時に途中姿勢を作り込まず、完全に開いた状態で位置、回転、寸法を決めます。
収納方法は保存せず、開姿勢の接地、傾き、中央線との交差から自動導出します。
紙面から離れた部品は小口の外側を迂回しながら収納されます。

見開き紙面より下へ部品を置くことはできません。
ルート部品は空中にあっても左右どちらかのページに属し、中央線を越えて移動すると開姿勢を保ったまま所属ページが切り替わります。

部品は紙面に収まり、閉じるときに他の紙を貫通しない位置へ置きます。
紙面へ接したまま中央線をまたぐ平面部品は、起立・平置き・文字・パーティクルの別を問わず中央線で谷折りされます。
紙面へ接していない平面部品は中央線折りより外側迂回を優先し、ページ送り中に対向ページを横切らせません。
画像、文字、背景色、パーティクルは一つのビジュアル部品へ重ねられます。
パーティクルは奥行きを持たない矩形上へ配置し、ほかのビジュアル部品と同様に回転できます。
綴じ目付近は閉じたときの隙間が小さいため、大きな部品や空中の部品を密集させません。
背景プリセットは画像の幅を見開き幅へ合わせ、縦横比を保って紙面の奥へ接地する作成ショートカットです。
背景パネルは部品名またはアセットIDに `backdrop`、`background`、`背景`、`遠景` などの語を含め、本文以外の起立部品の中で最も高い `layer` にします。
`stow.stagger` は0にして展開時の起立開始を遅らせません。
他の部品は背景パネルの下端より上へ初期配置せず、背景パネルより奥側または同じ奥行きへ置きません。
配置後は `node scripts/verify-stow-layout.mjs <作品フォルダ> --strict` と保持・ページ遷移のスクリーンショットで、起立中の背景パネル貫通を確認します。

時間軸は次の2種類です。

- **オーサードタイムライン**：見開きの保持中に進む演出
- **Content MotionまたはResident Time**：部品が表示されている間に続く周期運動

効果音はタイムライン上の点です。
前へ連続再生してキューを跨いだときだけ鳴り、逆再生、スクラブ、停止中には鳴りません。

## リポジトリ内の作品をコードで作る

`projects/` の公開サンプルは生成物です。
`projects/*/project.json` を直接編集すると次の生成で消えるため、定義または上書きを変更します。

- 作品の構造は `scripts/samples/<work>.mjs` に書く
- 画面で調整した位置や寸法は `scripts/samples/overrides/<work>.json` に書く
- 素材の実体は `scripts/samples/assets/<work>/` に置く
- 新しい公開サンプルを追加するときは `scripts/generate-samples.mjs` のimportと `BUILDERS` に登録する

上書きファイルのキーは `見開きID/要素ID` です。
値には `position`、`rotation`、`scale`、`width`、`height`、`layer`、`opacity`、`visible`、`asset`、`remove` を使えます。
要素を削除すると、参照されなくなった素材も生成結果から除かれます。

生成と検査は続けて実行します。

```bash
npm run samples:generate
npm run samples:check
```

包含検査が失敗した場合は、エラーに表示される幅、高さ、位置の許容範囲へ配置を寄せます。
検査を無効化したり、生成後の `project.json` を直したりしません。

## 紙工作の座標

サンプル定義では世界座標を直接組み立てず、`scripts/samples/shared.mjs` の紙工作ヘルパーを使います。

- `flat`：紙面接着
- `stand`：接地線で立てる起立部品
- `hover`：紙面から離れた部品を置く
- `caption`、`signText`：文字を持つ部品
- `sparkle`：光の欠片
- `pivotGroup`：子部品を持つ回転軸

片面内の位置は正規化座標 `u` と `v` で指定します。

- `u=0`：背表紙
- `u=1`：小口
- `v=0`：紙面の奥
- `v=1`：紙面の手前

紙面に収まらない配置や畳めない高さはヘルパーが拒否します。
紙面の寸法を基準にし、エラーを回避するために検査条件を緩めません。

## 縮尺

部品の寸法は `scaleOf(metersPerUnit)` と `REAL` を使い、実物の比率から決めます。
同じ縮尺の中で犬、人物、建物などを個別に見栄えだけで拡大しません。

縮尺は紙面に描かれた敷石や落ち葉などの粒から逆算します。
手前と遠景など、舞台の役割が異なる領域は別の縮尺に分けられます。
ただし、同じ縮尺に属する物どうしの比率は保ちます。

遠景は絵の性質に合わせて作ります。
木立のような繰り返し模様は、片面幅の立ち板を左右へ1枚ずつ置けます。
家並みや稜線のような連続するパノラマは、中央線をまたぐ `stand` 1枚で作ります。
収納コンパイラが中央線交差を検出し、左右ページへ従う二翼へ自動分割します。
左右へ分割すると綴じ目で絵が切れ、片面幅によって高さも制限されます。

見出し用の白い板を置く場合は `signText` で文字を入れ、空の板を装飾として並べません。

## 画像素材

公開サンプルへ同梱する画像の実体はWebPに統一します。
`.mjs` 内のSVGは寸法とレイアウトを宣言する下書きであり、`art()` は同名のWebPを読み込みます。

任意の入力画像は次のスクリプトでサンプル素材へ取り込めます。

```bash
node scripts/adopt-alt-asset.mjs <work> <入力画像> <出力名.webp> --width W
```

紙面背景には `--page` を使います。
必要に応じて `--keep-top` または `--keep-bottom` で帯を切り出します。
この処理にはImageMagickの `magick` が必要です。

部品の縦横比は実WebPから取得します。
`wide(高さ, 素材ID)` を使い、画像のピクセル寸法を定義へ重複して書きません。

画像の周囲には透明な余白を残しません。
余白があると世界寸法が画像枠全体へ割り当てられ、絵が小さくなり、立ち板が接地線から浮きます。

```bash
node scripts/trim-assets.mjs          # 変更内容を確認
node scripts/trim-assets.mjs --apply  # 切り抜きを適用
```

差し替わる連番、パノラマの左右、重ねる画像は共通の枠を保ちます。
回転する部品は画像枠の中心を維持します。

表紙の英字タイトルは画像へ焼き込みます。

```bash
node scripts/bake-cover-titles.mjs <原画フォルダ>
node scripts/bake-cover-titles.mjs <原画フォルダ> --preview-dir <出力先>
```

入力には文字の入っていない原画を使います。
焼き込み済みの `scripts/samples/assets/` を入力にすると文字が重なるため指定しません。

## 音

サンプルで共通する音源は `scripts/samples/assets/audio/` に置きます。
`work.bgm('bgm.mp3')` は作品に1つのBGMを設定し、冒頭からループ再生します。
`work.pageTurns('page-turn.wav')` は、各見開きの保持区間の終わりにページをめくる音を置きます。

最後の見開きにはページめくり音を置きません。
最後に起きるのはページ送りではなく表紙を閉じる動作だからです。
個別の効果音は `s.cue(素材ID, [時刻])` で置きます。

音声は1ファイル3MB以内にします。
効果音の重なりは許容し、BGMは作品に1つだけ設定します。

## 作品内の文言

公開サンプル中の `caption`、`signText`、catalogの `title` と `description` は英語で書きます。
個人作品の文言は作品の対象読者に合わせます。

コード注釈とコミットメッセージは日本語で書きます。

## 作品の検査

作品の見た目だけでなく、機械検査で次の条件を確認します。

- 画像がWebPである
- 音声が1ファイル3MB以内である
- 1作品の素材合計が12MB未満である

次の2つは目視とスクリーンショットで確かめる項目です。作品によっては意図して外すので、
テストの失敗としては扱いません。

- 部品が閉じる途中で紙を貫通しない
- 保持中の全時刻と全モーション位相で部品が紙面に収まる

単一の進行値を画像に出す場合は次のコマンドを使います。

```bash
node scripts/screenshot.mjs --project projects/forest_lantern --scroll 0.5 --out shots/mid.png
```

保持区間とページ送りをまとめて確認する場合は次のコマンドを使います。

```bash
npm run qa:holds -- --out shots/holds --phases 0.5 --turns
```

背景パネルの積層順と初期配置だけを点検する場合は次のコマンドを使います。

```bash
node scripts/verify-stow-layout.mjs projects/<作品名> --strict
```

公開サンプルの演出内容を変えた場合は `src/schema/bookPackage.test.ts` の構造検査も更新します。
