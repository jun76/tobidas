# AGENTS.md

このファイルは、tobidasを使って作品を作るエージェント向けの入口です。

tobidas本体のソース、保存形式、ビルド、デプロイを変更する場合は、作業前に `AGENTS_DEV.md` も読んでください。
作品データと素材だけを扱う場合は、このファイルと作品の制作ガイドを使います。

## 作品ごとの制作ガイド

制作ノウハウの正本は、作品データに保存される `authoringGuide` です。
標準ビルダー右側のインスペクター下にある「制作ガイド」ペインを開くと、表示言語ごとの項目ラベル、説明、編集欄を確認できます。
新規作品にはtobidasの既定ガイドが複製されますが、作品ごとに利用者が編集できます。

作品を作り始める前に制作ガイドを読み、そこに書かれた方針を現在の作品の基準として扱ってください。
WebMCPが使える場合は `tobidas-get-authoring-guide` を使い、更新は利用者から明示的に依頼された場合だけ `tobidas-update-authoring-guide` を使います。
WebMCPが使えない場合は、標準ビルダーの制作ガイドペインを読みます。
このファイル、README、スキルの本文へ作品づくりの既定値を複製しません。

## 作品制作の共通手順

1. 初回の依頼から設計シートを作り、未確定で結果が変わる事項だけ質問します。
2. 制作ガイドと確認済みの設計シートから、必要な素材と見開き構成を決めます。
3. 新規作品を作成し、素材は標準のアセットパネルから読み込みます。
4. BOOKナビゲーターで対象の見開きを選び、プリセットまたは詳細操作で部品を配置します。
5. インスペクターとタイムラインで内容、演出、音を設定します。WebMCPが使える場合も同じ共通操作を使います。
6. 保存後に、利用者が指定した場所へ作品フォルダを置きます。
7. 標準ビルダーの再生とスクリーンショットで、作品全体を確認します。

作品の途中姿勢や収納方法は作品データへ直接書き込みません。配置後の検証と再生で問題が見つかった場合は、制作ガイド、設計、素材、配置のいずれかを見直します。

## ブラウザ操作AIとWebMCP

ブラウザ操作AIと利用者は、同じ標準ビルダーを操作します。
WebMCPが使える場合は構造化ツールを追加し、使えない場合は意味付きDOM、ARIA、詳細操作へフォールバックします。
WebMCPの有無で作品データ、検証、undo、自動保存の経路を変えません。

アセットのアップロード、作品の開く・保存・エクスポートは標準UIを使います。WebMCPへdata URL、Blob、ファイルハンドル、IndexedDBの内部構造を渡しません。
WebMCPの編集ツールは `src/builder/operations/commands.ts` の共通コマンドを通し、任意JSON置換やDOMへの直接変更を行いません。
利用可能なツールとブラウザごとの条件は[READMEのブラウザ操作AIの章](./README.md#ブラウザ操作aiから使う)を参照してください。

## リポジトリ内の作品をコードで作る

`projects/` の公開サンプルは生成物です。
`projects/*/project.json` を直接編集すると次の生成で消えるため、定義または上書きを変更します。

- 作品の構造は `scripts/samples/<work>.mjs` に書く
- 画面で調整した位置や寸法は `scripts/samples/overrides/<work>.json` に書く
- 素材の実体は `scripts/samples/assets/<work>/` に置く
- 新しい公開サンプルを追加するときは `scripts/generate-samples.mjs` のimportと `BUILDERS` に登録する

上書きファイルのキーは `見開きID/要素ID` です。
値には `position`、`rotation`、`scale`、`width`、`height`、`layer`、`opacity`、`visible`、`asset`、`remove` を使えます。
要素を削除すると、参照されなくなった素材も生成結果から除かれます。

```bash
npm run samples:generate
npm run samples:check
```

生成後の `project.json` を直接直したり、包含検査を無効化したりしません。

## サンプル定義と素材の技術規約

サンプル定義では `scripts/samples/shared.mjs` の紙工作ヘルパーを使い、世界座標を重複して組み立てません。
画像部品の寸法は実素材から取得し、SVGの下書きとWebPの実体を分離します。
公開サンプルの画像はWebPへ統一し、音声は共通アセットまたは作品固有のアセットとして管理します。

作品の素材を取り込む場合は、対応形式、透過、余白、アスペクト比、ファイルサイズを確認します。
作品の素材合計は12MB未満、音声は1ファイル3MB以内にします。コード注釈とコミットメッセージは日本語で書きます。

## 作品の検査

見た目の確認には標準ビルダーの再生、スクリーンショット、Browser／Computer Useを使います。
構造検査には次を使います。

```bash
node scripts/screenshot.mjs --project projects/<作品名> --scroll 0.5 --out shots/mid.png
npm run qa:holds -- --out shots/holds --phases 0.5 --turns
node scripts/verify-stow-layout.mjs projects/<作品名> --strict
```

公開サンプルの演出内容を変えた場合は、次も実行します。

```bash
npm run samples:generate
npm run samples:check
```

構造検査は配置、包含、収納、タイムラインなど機械的に判定できる範囲を確認します。
作品の構図、読みやすさ、素材の重なり、演出の良否はスクリーンショットで確認し、問題があれば制作ガイドと作品を修正します。
